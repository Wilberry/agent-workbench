name: Runner Matrix Probe

on:
  pull_request:

jobs:
  probe_arm_linux:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: echo "arm linux runner acquired"

  probe_macos:
    runs-on: macos-15
    steps:
      - run: echo "macos runner acquired"
