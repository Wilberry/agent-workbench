{
  "name": "agent-workbench",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "vitest",
    "test:unit": "vitest run tests/**/*.test.ts",
    "test:e2e": "vitest run tests/e2e/**/*.ts"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "typescript": "^5.2.2",
    "turbo": "^1.12.1",
    "prettier": "^3.1.0",
    "husky": "^9.0.0",
    "vitest": "^0.40.1",
    "dotenv": "^16.3.1"
  }
}
