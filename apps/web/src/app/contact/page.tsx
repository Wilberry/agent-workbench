import { ContactClient } from './ContactClient';

export const metadata = {
  title: 'Contact | Agent Workbench',
  description: 'Contact Agent Workbench for contribution inquiries, general questions, bug reports, and feature requests.'
};

export default function ContactPage() {
  return <ContactClient />;
}
